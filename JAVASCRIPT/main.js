const fruits = ["banana", "mango", "ananas"] // starting arry

fruits.unshift("strawberry") // adds the element to start

fruits.push("strawberry") // adds the element to end

fruits.pop() // removes and returns the last element of the array

console.log(fruits)